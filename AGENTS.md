# VirtualTissue

Maintain one Manual v3-driven gut workflow, individual Jev choices and local-only
sensing. All public text is English. Preserve the six Diorama.cell morphologies;
monocyte/DC meshes are separate. Do not modify the immutable manual_v2 reference.
The active manual_v3 kernel is derived from that tested reference.

Read README.md, manual_v3/Manual_v3.md and docs/TISSUE_PACKS.md. Pack/rule changes
must be consumed by host and server. New biology needs a sourced contract, actual
physical handler and tests. Unsupported actions stay blocked. Do not conflate
ETEC secretion with EPEC contact injury or add a rule-mode selector.

Read the official TypeSafe skill/current docs before changing the adapter.
No paid calls without explicit authorization and an agreed budget. Automated
tests use fixtures only. Never expose keys or include private runs in releases.

Record every decision independently of visual sampling. Preserve partial failures
and honest usage. Playback is offline data only. Run build.py after source/pack
changes, then scripts/check_all.py including browser/episode suites. Package only
with scripts/package_release.py. Publish only the clean export when authorized;
never push a working checkout containing local development artifacts.
