# VirtualTissue 6.1.1

The active release is governed by Manual v3, with ETEC, EPEC, IBD-like innate injury
and quiet baseline. Both server and host consume the same compiled pack. There
are 56 specification contracts and 22 supported physical handlers. Unsupported
processes remain blocked. Biological parameters and Jev choices are uncalibrated.

Run `python scripts/check_all.py` for current verification; historical pass counts
are not release evidence. All automatic tests and distributed recordings use
fixtures. See README for commands and docs/RELEASE.md for the clean export.
Only the generated allowlisted export should enter the new public repository.
